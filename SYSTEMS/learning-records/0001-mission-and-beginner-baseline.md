# Mission established; true-beginner backend baseline

The SYSTEMS mission is set (2026-07-19): system design interviews at AI labs as the checkpoint, real capability to build production backends/platforms/infra as the goal — the same dual structure as the DSA track. The user self-assessed as a true beginner on backends: HTTP servers, databases, caching, and load balancers are unfamiliar or fuzzy. Style preference is mixed (interactive design lessons + occasional local hands-on in Python); cadence is parallel with DSA inside 1–2 h/day.

**Implications:** Start from the client–server model with nothing assumed; introduce vocabulary via the glossary as it appears. Hands-on stays local-machine and Python-only (http.server, curl, later FastAPI/SQLite/Redis-style tools). Do not schedule DDIA reading or Gossip Glomers until foundations (phase 1) show evidence in quiz results. Reuse the DSA course's interactive-visual convention — it's a proven preference, per `../DSA/NOTES.md`.
